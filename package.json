{
  "name": "@scoretravel/takehome",
  "type": "module",
  "version": "0.0.1",
  "sideEffects": false,
  "engines": {
    "node": ">=20.15",
    "pnpm": "9.x"
  },
  "scripts": {
    "test": "vitest --run src",
    "test:watch": "vitest src",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
